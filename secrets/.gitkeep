This directory is mounted into the containers as /run/secrets.

Each secret is one file whose name is the secret name. The directory is
mounted, never the individual files: see docs/04-operations.md.

Real values are never committed. scripts/bootstrap.sh writes a local
development key here, and .gitignore excludes everything except this file.
