---
description: Analyse the task brief into requirements, traps, and assumptions
---

The brief is open. Do not write any code yet.

1. Invoke the `brief-analyst` agent on the brief.
2. Write its output to `docs/00-brief-analysis.md`. Keep the requirement
   identifiers stable; everything for the rest of the session refers to them.
3. Read the traps section aloud as you go. This is the part that demonstrates
   judgement, and it is the part a reviewer scores hardest.
4. If the reference match is below seventy per cent, say so plainly and ignore
   the reference kit for the rest of the session.

Then invoke `planner`, write `docs/01-plan.md`, and let the automatic
adversarial pass run before anything is built.

Time check: this whole step ends at minute eight.

$ARGUMENTS
